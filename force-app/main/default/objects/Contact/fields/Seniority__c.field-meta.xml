<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Seniority__c</fullName>
    <externalId>false</externalId>
    <formula>IF(MONTH(TODAY())&gt;MONTH(joinDateBS__c),YEAR(TODAY())-YEAR(joinDateBS__c),IF(AND(MONTH(TODAY())=MONTH(joinDateBS__c),DAY(TODAY())&gt;=DAY(joinDateBS__c)),YEAR(TODAY())-YEAR(joinDateBS__c),(YEAR(TODAY())-YEAR(joinDateBS__c))-1))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Seniority</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
